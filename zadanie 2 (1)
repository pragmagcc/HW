#include<iostream>
#include <ctime>
/*процент ((последнее -  певое) + 1)*/

using namespace std;

int Dice(){
	int n = rand() % 6 + 1;
	return n;
}


int main(){
	srand(time(0));
	cout << Dice() << " " << Dice();
	
	return 0;
}
